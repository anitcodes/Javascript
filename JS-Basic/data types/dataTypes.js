// Data Types Practice Questions:

// 1. What data type is returned by typeof "Hello"?
/*
let greet = "Hello";
console.log(typeof(greet)); //string

// 2.Add Two Numbers from User
// Ask the user to input two numbers using prompt() and print their sum as a number, not string.

let num1 = Number(prompt("Enter a number"));
let num2 = Number(prompt("Enter another number"));

if(isNaN(num1) || isNaN(num2)){
    alert("Please enter a valid number");
}else{
    let sum = num1 + num2;
    alert(`Sum = ${sum}`);
}

// 3.Check If Input is a Number
// Ask the user to enter any value. Show whether the input is a number or not.

let input = prompt("Enter a value");
if(isNaN(input) && input.trim() !== ""){ //NaN check if the given is Not a Number
alert(`${input} is not a number`)
}else{
    alert(`${input} is a number`)
}
*/


// 4.Type Checker Function
// Write a function checkType(value) that logs whether the value is a number, string, boolean, object, or undefined.

// 5.Truthy/Falsy Tester
// Create an array of values: ["", "0", 0, null, undefined, "Anit", true, false].
// Loop through the array and log whether each value is truthy or falsy.

// 6.Convert Age String to Number
// Let age = "20". Convert it to a number and then add 5. Log the result.

// 7.Object Property Data Types
// Create a person object with properties: name (string), age (number), isMarried (boolean), children (null).
// Print each value and its type.

// 8.Student Array
// Create an array of students’ names and check:
// Is it an array?
// What’s its data type using typeof?

// 9.Calculate Total Price
// Let price = "100", quantity = 2. Convert price to a number and print total price.

// 10.Boolean Conversion
// Convert the following values to boolean using Boolean():
// "Anit", "", 0, 1, null, undefined, [], {}
// Print the results.

// 11.Detect NaN
// Try dividing "hello" by 5 and check if the result is NaN using isNaN().


// 💡 Challenge Tasks

// 12.Auto Type Converter
// Create a function that takes a string input like "true", "123", "null", "undefined" and converts it to its real JavaScript data type.

// 13.Guess the Output
// Predict the output before running:
// let x = "10";
// let y = 5;
// let result = x * y;
// console.log(result, typeof result);


// 14.User Details Logger
// Ask the user their name, age, and isStudent (yes/no). Store these values in an object and log the types of each.

// 15.Mix Array Types
// Create an array: ["Nepal", 123, true, null, undefined].
// Loop and print value + type of each element.












