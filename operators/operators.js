//Practice questions of Operators

// 1. Write a program that adds two numbers and prints the result.

// let num1 = 2;
// let num2 = 3;

// let sum = num1 + num2;
// console.log("Sum = " + sum);

//taking input from the user in browser
// let num1 = Number(prompt("Enter the first number: "));
// let num2 = Number(prompt("Enter the second number: "));

// let sum = num1+num2;

// alert("Total Sum = " + sum);

// 2. Use the ternary operator to check if a number is even or odd.

// const num = 5;
// const result = (num%2 === 0) ? "Even" : "Odd"; //using ternary operator
// console.log(result);

// 3. What will this return?

// let a = 10;  
// a += 5;             //a = a + 5; -> 10 + 5 = 15
// console.log(a);

/*
### Practical Operator Questions

1. Ask the user to enter two numbers and **display their sum, difference, product, quotient, and remainder

2. Get the length and breadth of a rectangle from the user and **calculate the area

3. Calculate and show the value of **2 raised to the power of 6

4. Ask the user for a number and check if it is even or odd** using the modulus `%` operator.

5. Start with a salary of 25,000. Add a bonus of 7,500 using  and show the new salary.

6. You have 15 items in stock. 4 items are sold. **Update and display the remaining stock using `-=`.

7. Ask the user for their age and check if they are eligible to vote (age ≥ 18

8. Prompt the user to enter a password and check if it exactly matches a preset password.

9. Compare the string `"100"` and number `100` using `==` and `===`. Print both results.

10. Ask the user to input two numbers and display the larger number using comparison operators.

11. Ask for username and password. Allow login only if both match predefined values** using logical AND (`&&`).

12. Check if a user qualifies for a discount if they are a student OR their purchase is above 1000 using logical OR (`||`).

13. If a variable `isAdmin` is falseprint a message using the NOT operator (`!`).

14. Start with `likes = 50`. Simulate a like button press by incrementing likes using `++` and show the result.

15. Count down from 5 to 0 using the decrement operator (`--`) and print each number.

16. Create a simple ATM program where the user can deposit or withdraw money updating the balance using assignment operators.

17. Ask for a student's score and **assign a grade based on their score using comparison operators** (A, B, C, F).

18. You have a shopping cart with 3 items. The user adds 2 more and removes 1. **Update and show the final quantity using `+=` and `-=`.
*/